class Studant {
  matricula: string;
  nome: string;
  notasProva: number[];
  notasTrabalho: number[];

  constructor(
    matricula: string,
    nome: string,
    notasProva: number[],
    notasTrabalho: number[],
  ) {}
}